version: '3.3'

services:
  email_agent:
    build: .
    container_name: gemini_email_agent
    restart: unless-stopped
    # No ports needed since it's a Telegram polling bot
    env_file:
      - .env
